/* VPC CIDR block - 10.0.0.0/16 
2 subnets - 1 public(subnet1), 1 private(subnet2) each 10.0.0.0/24 or 10.0.1.0/24
add igw
add route table and routes
add route table association for public subnet to route table
 */